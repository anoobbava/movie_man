import 'package:flutter/material.dart';

class SearchMovies extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('welcome to Search Movies'),
      ),
    );
  }
}
